#summary Documentation for the driver.c file

= driver.c =



== Functions ==

void _list_destroy(struct  ** list, int(*)(struct  *) fn)
  **
void _list_push(struct  ** list, struct  * connector)
  **
void _list_remove(struct  ** list, struct  * connector, int(*)(struct  *) fn)
  **
int _receiver_connect(void * driverp, struct  * receiver)
  **
int _receiver_disconnect(void * driverp, struct  * receiver)
  **
int _sender_relay(void * driverp, struct  * message)
  **
int _sender_connect(void * driverp, struct  * sender)
  **
int _sender_disconnect(void * driverp, struct  * sender)
  **

== Detailed Description ==




== Function Documentation ==

{{{
#void _list_destroy   (
  struct  ** list,
  int(*)(struct  *) fn  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#void _list_push   (
  struct  ** list,
  struct  * connector  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#void _list_remove   (
  struct  ** list,
  struct  * connector,
  int(*)(struct  *) fn  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int _receiver_connect   (
  void * driverp,
  struct  * receiver  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int _receiver_disconnect   (
  void * driverp,
  struct  * receiver  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int _sender_relay   (
  void * driverp,
  struct  * message  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int _sender_connect   (
  void * driverp,
  struct  * sender  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int _sender_disconnect   (
  void * driverp,
  struct  * sender  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

---
|| [MIDIKit_docs Main Page] || [MIDIKit_docs_files Files] ||
