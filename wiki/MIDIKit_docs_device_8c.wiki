#summary Documentation for the device.c file

= device.c =



== Functions ==

int _connect(struct  ** device_connector, struct  * connector)
  **
int _disconnect(struct  ** device_connector)
  **
int _in_relay(void * devicep, struct  * message)
  **
int _in_connect(void * devicep, struct  * in)
  **
int _in_disconnect(void * devicep, struct  * in)
  **
int _out_connect(void * devicep, struct  * out)
  **
int _out_disconnect(void * devicep, struct  * out)
  **
int _thru_connect(void * devicep, struct  * thru)
  **
int _thru_disconnect(void * devicep, struct  * thru)
  **

== Detailed Description ==




== Function Documentation ==

{{{
#int _connect   (
  struct  ** device_connector,
  struct  * connector  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int _disconnect   (
  struct  ** device_connector  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int _in_relay   (
  void * devicep,
  struct  * message  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int _in_connect   (
  void * devicep,
  struct  * in  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int _in_disconnect   (
  void * devicep,
  struct  * in  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int _out_connect   (
  void * devicep,
  struct  * out  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int _out_disconnect   (
  void * devicep,
  struct  * out  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int _thru_connect   (
  void * devicep,
  struct  * thru  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int _thru_disconnect   (
  void * devicep,
  struct  * thru  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

---
|| [MIDIKit_docs Main Page] || [MIDIKit_docs_files Files] ||
