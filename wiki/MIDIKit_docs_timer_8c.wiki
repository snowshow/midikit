#summary Documentation for the timer.c file

= timer.c =



== Functions ==

struct  * MIDITimerCreate(struct  * delegate)
  **
void MIDITimerDestroy(struct  * timer)
  **
void MIDITimerRetain(struct  * timer)
  **
void MIDITimerRelease(struct  * timer)
  **
int MIDITimerReceive(struct  * timer, struct  * device, struct  * message)
  **
int MIDITimerSend(struct  * timer, struct  * device, struct  * message)
  **

== Detailed Description ==




== Function Documentation ==

{{{
#struct  * MIDITimerCreate   (
  struct  * delegate  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#void MIDITimerDestroy   (
  struct  * timer  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#void MIDITimerRetain   (
  struct  * timer  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#void MIDITimerRelease   (
  struct  * timer  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int MIDITimerReceive   (
  struct  * timer,
  struct  * device,
  struct  * message  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#int MIDITimerSend   (
  struct  * timer,
  struct  * device,
  struct  * message  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

---
|| [MIDIKit_docs Main Page] || [MIDIKit_docs_files Files] ||
